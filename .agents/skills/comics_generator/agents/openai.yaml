interface:
  display_name: "Comics Generator"
  short_description: "Repo-specific patterns and workflows for comics_generator"
  default_prompt: "Use the comics_generator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true